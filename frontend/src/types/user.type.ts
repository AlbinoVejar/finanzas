export type UserStateType = {
  idUser: number;
  filters: {
    init_date: string;
    end_date: string;
  }
}

export type UserDashboard = {
  Id: number;
  Id_account: number;
  Init_date: string;
  End_date: string;
}